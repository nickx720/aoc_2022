use super::GlobalError;

pub fn run() -> Result<(), GlobalError> {
    Ok(())
}

#[test]
fn day_13_challenge_one() -> Result<(), GlobalError> {
    assert_eq!(1, 1);
    Ok(())
}
#[test]
fn day_13_challenge_two() -> Result<(), GlobalError> {
    assert_eq!(1, 1);
    Ok(())
}
